<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="cn.scud.main.order.dao.OrderDao">
    <cache />


    <!--保存订单-->
    <insert id="saveOrder" parameterType="Order">
        INSERT INTO order (order_userid, order_createtime,order_complteflag,order_token,order_content,order_acptusersex,order_commission,order_limittime,order_serviceaddress,order_callscope,order_pictures)
        VALUES (#{orderUserId}, #{orderCreateTime},#{orderComplteFlag},#{orderToken},#{orderContent},#{orderAcptUserSex},#{orderCommission},#{orderLimitTime},#{orderServiceAddress},#{orderCallScope},#{orderPictures})
    </insert>


</mapper>