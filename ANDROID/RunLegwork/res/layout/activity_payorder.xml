<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent" >
    <com.team.dream.runlegwork.widget.MainTitileBar
        android:id="@+id/payorder_topbar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content" />
    <LinearLayout 
        android:id="@+id/payorder_llPay"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:layout_alignParentBottom="true"
        >
        <TextView 
            android:id="@+id/payorder_tvAllmoney"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:padding="@dimen/default_padding"
            android:layout_weight="3"
            android:text="总计"
            android:gravity="center|right"
            android:background="@drawable/tab_1"
            />
        <TextView 
            android:id="@+id/payorder_tvConfirm"
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:padding="@dimen/default_padding"
            android:layout_weight="2"
            android:gravity="center"
            android:textColor="#FFFFFF"
            android:background="@color/boy"
            android:text="确认下单"
            />
    </LinearLayout>
    <ListView 
        android:id="@+id/payorder_listv"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@id/payorder_topbar"
        android:layout_above="@id/payorder_llPay"
        ></ListView>

</RelativeLayout>
